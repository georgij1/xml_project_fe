// import { setPage, setRowsPerPage } from "../objects/TableSettingsObjects";

export const handleChangeRowsPerPage = (event: React.ChangeEvent<HTMLInputElement>) => {
    // setRowsPerPage(parseInt(event.target.value, 10));
    // setPage(0);
};